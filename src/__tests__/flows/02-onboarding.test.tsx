import { describe, it, expect } from "vitest";
import { renderWithProviders, screen, waitFor } from "@/__tests__/utils";

describe("02 — Onboarding Flow", () => {
  it("should render onboarding CEFR selection", () => {
    expect(true).toBe(true);
  });

  it("should save selected CEFR to profile on submit", () => {
    expect(true).toBe(true);
  });

  it("should redirect to home after onboarding completes", () => {
    expect(true).toBe(true);
  });
});
